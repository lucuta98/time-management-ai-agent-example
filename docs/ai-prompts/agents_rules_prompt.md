# Prompt 1

Create a file named AGENTS.md inside the .bob/rules-plan folder.
This file must define a rule for Plan mode.
Whenever Plan mode is used to generate a requirements .md file, automatically create a prompt file in docs/ai-prompts/ named 01_requirements_prompt.md (or matching the subject).
Also automatically create the same prompt file inside docs/requirements/ with the same name.
The rule must trigger every time Plan mode is used for generating planning files.

# Prompt 2

Create a new file in docs/ai-prompts/.
Name it agents_rules_prompt.md.
Put inside it the full prompt I wrote about creating AGENTS.md in .bob/rules-plan.
Do not modify the text.
Just save it exactly as a prompt file.